# tools/

여기에는 실제로 일을 하는 파이썬 파일이 들어간다. API 호출, 데이터 변환, 리포트 생성, 이메일 발송 등.

규칙:

- Tool 하나는 한 가지 일만 잘한다. 여러 Workflow에서 재사용하는 게 목표.
- 새 Tool을 만들기 전에 이 폴더에 비슷한 게 이미 있는지 먼저 확인한다.
- 비밀 값(API 키 등)은 절대 이 안에 직접 적지 않는다. `.env`에서 읽어온다.
